display_information:
  name: transcription-alert-bot
  description: Alert bot for the transcription pipeline
  background_color: "#394054"

features:
  bot_user:
    display_name: TranscriptionMonitor
    always_online: false

oauth_config:
  scopes:
    bot:
      - chat:write
      - files:write

settings:
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
